/node_modules
/.env.local
/dist
.swc

# OpenAPI 生成类型（CI 动态生成）
/src/interfaces/generated/

# BFF 运行时数据（首次启动自动生成）
/bff/data/
# 例外:harness 配置与租户绑定可入库(无 token 明文,Constitution Token 安全约束)
!/bff/data/harness-backends.json
!/bff/data/bff-tenants.json

# 敏感环境变量(Constitution Token 安全约束)
/.env
/.env.local
/.env.*.local
/bff/.env

*storybook.log
storybook-static

# IDE / 工具本地配置(不入库)
.DS_Store
.trae/
.specify/
.agents/
bff/node_modules/.vite/

# 测试覆盖率产物
coverage/

# 临时文件
.tmp/
tmp_*.py

# 误装到 bff/node_modules 的非依赖目录(应通过 npm install 安装,不入库)
bff/node_modules/hono-rate-limiter/
